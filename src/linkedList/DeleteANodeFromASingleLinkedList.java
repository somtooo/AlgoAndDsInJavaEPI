package linkedList;

public class DeleteANodeFromASingleLinkedList {
  // just delete the successor(by making the current node point to the successors next node) and copy the value to the current node
}
